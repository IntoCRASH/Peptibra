import {redirect,notFound} from "next/navigation";import {getAuthorizedUser} from "@/app/chatgpt-auth";import {createClient} from "@supabase/supabase-js";import ProtocolPrint from "./ProtocolPrint";
export default async function ProtocolPage({params}:{params:Promise<{id:string}>}){if(!await getAuthorizedUser())redirect("/login?next=/ptbr-mobile");const {id}=await params,s=createClient(process.env.SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}}),{data:protocol}=await s.from("protocols").select("*").eq("id",id).maybeSingle();if(!protocol)notFound();const {data:product}=await s.from("products").select("*").eq("id",protocol.product_id).maybeSingle();return <ProtocolPrint protocol={protocol} product={product||{}}/>}
