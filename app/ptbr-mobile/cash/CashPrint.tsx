"use client";
import {useEffect,useState} from "react";import {CorporateFooter,CorporateHeader,PrintToolbar} from "../print-shared";
type Row=Record<string,unknown>;const money=(v:unknown)=>`US$${Number(v||0).toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default function CashPrint(){const [cash,setCash]=useState<Row[]>([]);useEffect(()=>{fetch("/api/mobile").then(r=>r.json()).then(x=>setCash(x.cash||[]))},[]);const total=cash.reduce((s,x)=>s+(x.type==="Ingreso"?1:x.type==="Egreso"?-1:0)*Number(x.amount||0),0);return <main className="print-document"><PrintToolbar title="Movimientos de Caja PEPTIBRA"/><article className="canonical-sheet text-report"><CorporateHeader kicker="" title="Movimientos de Caja PEPTIBRA"/><div className="report-lines"><p>PEPTIBRA - MOVIMIENTOS DE CAJA</p><p>Generado: {new Date().toISOString().slice(0,16).replace("T"," ")}</p><br/>{cash.map(x=><p key={String(x.id)}>{String(x.created_at).slice(0,16).replace("T"," ")} | {String(x.category)} | {x.type==="Ingreso"?"+":x.type==="Egreso"?"-":"≈"}{money(x.amount)}{x.partner_name?` | Socio: ${x.partner_name}`:""}{x.notes?` | ${x.notes}`:""}</p>)}{!cash.length&&<p>Sin movimientos registrados.</p>}<br/><p>SALDO NETO DE CAJA: {money(total)}</p></div><CorporateFooter left="PEPTIBRA  |  Página 1 de 1"/></article></main>}
