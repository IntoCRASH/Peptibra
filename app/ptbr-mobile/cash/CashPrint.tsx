"use client";
import Image from "next/image";import {useEffect,useState} from "react";
type Row=Record<string,unknown>;const money=(v:unknown)=>`US$${Number(v||0).toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default function CashPrint(){const [cash,setCash]=useState<Row[]>([]);useEffect(()=>{fetch("/api/mobile").then(r=>r.json()).then(x=>setCash(x.cash||[]))},[]);const total=cash.reduce((s,x)=>s+(x.type==="Ingreso"?1:x.type==="Egreso"?-1:0)*Number(x.amount||0),0),share=async()=>{const message=`Reporte de Caja Peptibra · Disponible ${money(total)}`;if(navigator.share){try{await navigator.share({title:"Reporte de Caja Peptibra",text:message,url:location.href});return}catch{}}window.open(`https://wa.me/?text=${encodeURIComponent(`${message}\n${location.href}`)}`,"_blank")};return <main className="invoice-print-page"><div className="print-toolbar"><button onClick={()=>window.print()}>Imprimir</button><button onClick={()=>window.print()}>Guardar PDF</button><button onClick={share}>Compartir / WhatsApp</button></div><article className="invoice-sheet cash-sheet"><header><Image src="/peptibra-logo-original.png" width={1774} height={887} alt="Peptibra" priority/><div><span>REPORTE DE CAJA</span><b>Movimientos</b><small>{new Date().toLocaleDateString("es-DO")}</small></div></header><section className="cash-report-total"><span>Disponible en caja</span><b>{money(total)}</b></section><table><thead><tr><th>Fecha / concepto</th><th>Tipo</th><th>Monto</th></tr></thead><tbody>{cash.map(x=><tr key={String(x.id)}><td><b>{String(x.category)}</b><small>{String(x.created_at).slice(0,10)} · {String(x.notes||x.partner_name||"")}</small></td><td>{String(x.type)}</td><td>{x.type==="Ingreso"?"+":x.type==="Egreso"?"−":"≈"}{money(x.amount)}</td></tr>)}</tbody></table><footer><span>peptibra.com</span></footer></article></main>}
