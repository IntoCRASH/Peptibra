"use client";
import {useEffect,useState} from "react";
import "../mobile-enhancements.css";
export default function SettingsClient(){const[retention,setRetention]=useState("20"),[partner,setPartner]=useState(false),[status,setStatus]=useState("");useEffect(()=>{fetch("/api/mobile").then(r=>r.json()).then(d=>{const get=(k:string,f:string)=>String((d.settings||[]).find((x:{key:string})=>x.key===k)?.value??f);setRetention(get("retencion_utilidad","20"));setPartner(get("ganancia_por_socio","0")==="1")})},[]);async function save(){for(const [key,value] of [["retencion_utilidad",retention],["ganancia_por_socio",partner?"1":"0"]])await fetch("/api/mobile",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"saveSetting",data:{key,value}})});setStatus("Configuración guardada")};async function restore(file:File){if(!confirm("El respaldo se combinará con los datos actuales. ¿Continuar?"))return;const body=await file.text(),r=await fetch("/api/mobile/backup",{method:"POST",headers:{"content-type":"application/json"},body}),j=await r.json();setStatus(r.ok?"Respaldo restaurado correctamente":j.error||"No se pudo restaurar")};return <main className="settings-mobile-page"><header><a href="/ptbr-mobile">← Volver</a><span>CONFIGURACIÓN</span><h1>Preferencias y respaldo</h1></header><section><label>Porcentaje que se guarda para el negocio<input type="number" min="0" max="100" step="0.1" value={retention} onChange={e=>setRetention(e.target.value)}/></label><label className="settings-check"><input type="checkbox" checked={partner} onChange={e=>setPartner(e.target.checked)}/> Separar ganancias e inventario por socio</label><button onClick={save}>Guardar preferencias</button></section><section><h2>Respaldo de datos</h2><p>Incluye productos, inventario, clientes, facturas, pagos, caja, equipo, proveedores, protocolos y configuraciones.</p><a className="settings-action" href="/api/mobile/backup" download>Exportar respaldo completo</a><label className="settings-action file">Importar / restaurar respaldo<input type="file" accept="application/json,.json" onChange={e=>e.target.files?.[0]&&restore(e.target.files[0])}/></label></section>{status&&<p className="settings-status">{status}</p>}</main>}
