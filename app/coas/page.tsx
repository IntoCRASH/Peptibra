import Link from "next/link";import SiteHeader from "../SiteHeader";import VerificationGate from "../VerificationGate";import {products} from "../productData";
export default function CoasPage(){return <main className="pb-site pb-public-new"><VerificationGate/><SiteHeader/><section className="new-page-head coa-head"><span>DOCUMENTACIÓN POR LOTE</span><h1>Centro de COAs</h1><p>Localiza el producto y solicita el reporte correspondiente al lote vigente.</p></section><section className="new-coa-list">{products.map((p,i)=>{const available=p.coaStatus==="Documentación disponible";const subject=encodeURIComponent(`Solicitud de COA · ${p.name}`);return <article key={p.slug}><b>{String(i+1).padStart(2,"0")}</b><div><small>{p.category}</small><h2>{p.name}</h2><p>{p.dose} · {p.format}</p></div><span className={available?"ready":"review"}>{available?"Disponible":"En revisión"}</span><a href={`mailto:peptibra@gmail.com?subject=${subject}`}>Solicitar COA →</a></article>})}</section><section className="new-coa-note"><h2>Cómo verificar un reporte</h2><p>Compara producto, presentación, número de lote, fecha del análisis y laboratorio emisor. El documento siempre debe corresponder al lote consultado.</p><Link href="/faq">Cómo interpretar los COAs →</Link></section></main>}
