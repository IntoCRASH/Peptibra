"use client";
import Image from "next/image";
import {useEffect,useMemo,useState} from "react";
import {useLanguage} from "./LanguageProvider";
import {products as editorial} from "./productData";
type Item={id:number;name:string;concentration:string;price:number;stock:number;photoKey?:string;category?:string};
export default function HomeCatalog(){const {locale}=useLanguage(),es=locale==="es",[items,setItems]=useState<Item[]>([]);useEffect(()=>{fetch("/api/catalog").then(r=>r.json()).then(j=>setItems(j.products||[])).catch(()=>{})},[]);const families=useMemo(()=>{const map=new Map<string,Item[]>();for(const item of items)map.set(item.name,[...(map.get(item.name)||[]),item]);return [...map.entries()].slice(0,5)},[items]);return <div className="pb-mini-grid">{families.map(([name,variants])=>{const photo=variants.find(v=>v.photoKey)?.photoKey,fallback=editorial.find(p=>p.name.toLowerCase()===name.toLowerCase())?.image||"/peptibra-logo-original.png",stock=variants.reduce((s,v)=>s+v.stock,0);return <article key={name}><div><Image src={photo?`/api/catalog/image?key=${encodeURIComponent(photo)}`:fallback} width={1254} height={1254} alt={`${es?"Producto":"Product"} ${name}`} unoptimized/></div><span>{variants[0].category||"Peptibra"}</span><h3>{name}</h3><p>{variants.map(v=>v.concentration).join(" · ")}</p><small>{stock>0?(es?`${stock} disponibles`:`${stock} available`):(es?"Agotado":"Sold out")}</small></article>})}</div>}
